export enum BackgroundTextureFillMode {
  /**
   * Adapt to width, scale based on texture width.
   */
  FitWidth,
  /**
   * Adapt to height, scale based on texture height.
   */
  FitHeight,
  /**
   * Scale mode, Scale the texture completely according to the canvas size.
   */
  Scale
}
