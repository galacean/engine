/**
 * Enumeration of input types.
 */
export enum InputType {
  /** Nothing. */
  None = 0,
  /** Wheel. */
  Wheel = 1,
  /** Mouse, touch, pen, etc. */
  Pointer = 2,
  /** Keyboard. */
  Keyboard = 4,
  /** Everything above. */
  All = 7
}
