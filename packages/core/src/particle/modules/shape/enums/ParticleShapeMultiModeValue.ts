/**
 * The mode used to generate new points in a shape.
 */
export enum ParticleShapeMultiModeValue {
  /** Generate points randomly. (Default) */
  Random,
  /** Animate the emission point around the shape. */
  Loop
}
