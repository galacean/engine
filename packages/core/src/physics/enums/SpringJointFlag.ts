/**
 * Flags for configuring the drive of a Spring Joint.
 */
export enum SpringJointFlag {
  /** Max distance enable */
  MaxDistanceEnabled = 2,
  /** Min distance enabled */
  MinDistanceEnabled = 4,
  /** Spring enabled */
  SpringEnabled = 8
}
