/**
 * Shader data grouping.
 */
export enum ShaderDataGroup {
  /** Scene group. */
  Scene,
  /** Camera group. */
  Camera,
  /** Renderer group. */
  Renderer,
  /** material group. */
  Material,
  /** render element group. */
  RenderElement
}
