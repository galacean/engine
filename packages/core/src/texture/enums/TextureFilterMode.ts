/**
 * The filter mode of the texture.
 */
export enum TextureFilterMode {
  /** Point filtering. */
  Point = 0,
  /** Bilinear fitering. */
  Bilinear = 1,
  /** Trilinear filtering. */
  Trilinear = 2
}
