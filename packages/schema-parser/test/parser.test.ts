import { parser } from "../src/Parser";
import { PluginManager } from "../src/plugins/PluginManager";

describe("parser test", () => {
  let pluginManager: PluginManager;
  beforeAll(() => {
    // pluginManager = (parser as any).pluginManager;
  });
  it("reset plugins", () => {
    // parser.resetPlugins();
    // expect((pluginManager as any).registeredPlugins.size).toEqual(0);
    // expect((pluginManager as any).plugins).toEqual([]);
  });
});
