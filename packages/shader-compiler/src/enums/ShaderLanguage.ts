// Local copy — keep numeric values in lockstep with
// `packages/core/src/shader/enums/ShaderLanguage.ts`.
// See `packages/shader-compiler/src/enums/README.md` for the rationale.

/**
 * Shader language belongs to.
 */
export enum ShaderLanguage {
  /** GLSL ES 100. */
  GLSLES100,

  /** GLSL ES 300. */
  GLSLES300
}
