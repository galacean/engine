import { describe, it } from "vitest";

describe("ProjectLoader Component Reference Tests", function () {
  // TODO: update project.json to new format (old one contains deprecated "Environment" asset type)
  it.skip("should load project successfully with component be deleted", async () => {});
});
