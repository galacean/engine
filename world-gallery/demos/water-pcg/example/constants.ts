/**
 * Shared example-level constants.
 *
 * Values here describe demo switching state rather than river-domain behavior.
 * River-specific enums stay in river/constants.ts so the engine water vocabulary
 * remains centralized.
 */
export enum WaterPreviewMode {
  Ocean = "ocean",
  River = "river"
}
