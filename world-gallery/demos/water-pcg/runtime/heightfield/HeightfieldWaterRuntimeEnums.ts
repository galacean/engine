/** Debug outputs exposed by the heightfield-water surface shader. */
export enum HeightfieldWaterDebugMode {
  Final = 0,
  BaseHeight = 1,
  BaseNormal = 2,
  SignedDistance = 3,
  Depth = 4,
  Flow = 5,
  WaveDisplacement = 6
}
